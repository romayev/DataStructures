public class Main {

    // Create an int array of distances and initialize with values, i.e. 5, 3, 4, 2...
    String[] exits = { 1, 2, 3, 4, 5, 6};
    String[] distances = {5, 3, 4, 2, 8, 6};

    public static void main(String[] args) {
	// write your code here
        int exitAmount = exits.length;
        int total;

        for (int i= 0; exitAmount<6; i++}{
            exits[i]= exits[i] + exits [i+1];
            exits[i]  = new String distance[];
           if (i = total){

               System.out.println(exits);

            }
        }
    }

    int calculateDistance(int fromExit, int toExit) {
        int distance = 0;

        // iterate distances array from 'fromExit' to 'toExit'
        // sum distances into the 'distance' variable

        return distance;
    }
}
